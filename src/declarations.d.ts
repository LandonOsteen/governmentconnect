interface Contact {
  firstName: string,
  lastName: string,
  title: string,
  stafferFor: string
  established: boolean,
  pending: boolean,
  accepted: boolean
}

interface User {
  firstName: string,
  lastName: string,
  title: string,
  stafferFor: string
}